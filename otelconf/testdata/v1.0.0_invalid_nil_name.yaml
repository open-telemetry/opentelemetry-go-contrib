file_format: "1.0"
disabled: false
tracer_provider:
  processors:
    - batch:
        exporter:
          otlp_http:
            protocol: http/protobuf
            endpoint: http://localhost:4318/v1/logs
            headers:
              - name: api-key
                value: "1234"
              - value: nil-name
